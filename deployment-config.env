# RAILWAY PRODUCTION CONFIGURATION
# Copy these values into your Railway dashboard

# Core Application
NODE_ENV=production
PORT=5000
HOST=0.0.0.0
FORCE_HTTPS=true

# Generated Security Secrets (NEVER share these!)
SESSION_SECRET=a1e6d64dadc3e52cf7903e965bfc53cf2392cbcfae0c99d9d7bcaa83e4d9d3c8
CSRF_SECRET=74f23ad8eeb4579c3375b1ba097605b30c614d7c6a442aa69223a413fb0065a2

# Note: Your existing API keys will be copied from Replit:
# STRIPE_SECRET_KEY=(already configured)
# VITE_STRIPE_PUBLIC_KEY=(already configured) 
# OPENAI_API_KEY=(already configured)

# Database URLs (Railway will provide these automatically):
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# REDIS_URL=${{Redis.REDIS_URL}}

# Domain Configuration (replace with your Namecheap domain):
# DOMAIN=your-domain.com
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com